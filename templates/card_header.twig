
<header class="section-header--grid">
  <div cass="section-header__constrain">
    <h2 class="section-header__title">{{title}}</h2>
    <a class="section-header__link" href="{{url}}">{{link_text}}</a>
  </div>
  {% if intro %}
  <div class="section-header__intro">{{intro}}</div>
  {% endif %}
</header>