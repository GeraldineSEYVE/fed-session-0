
<div class="grid-4-col">
{% for grid in recent_section.items %}
    {% include "templates/card.twig" with {
        image: grid.image,
        category: grid.category,
        card_title: grid.title,
        card_link: grid.url,
        name_author: grid.authors
    }%}
{% endfor %}
</div>